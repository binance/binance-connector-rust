/*
 * Stocks Trading REST API
 *
 * REST APIs for Binance Stocks Trading. All endpoints under `/sapi/v1/equity/_*`.
 *
 * The version of the OpenAPI document: 1.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#![allow(unused_imports)]
use async_trait::async_trait;
use derive_builder::Builder;
use reqwest;
use rust_decimal::prelude::*;
use serde::{Deserialize, Serialize};
use serde_json::{Value, json};
use std::collections::BTreeMap;

use crate::common::{
    config::ConfigurationRestApi,
    models::{ParamBuildError, RestApiResponse},
    utils::send_request,
};
use crate::stocks::rest_api::models;

const HAS_TIME_UNIT: bool = false;

#[async_trait]
pub trait AccountApi: Send + Sync {
    async fn sign_us_equity_disclaimer(
        &self,
        params: SignUsEquityDisclaimerParams,
    ) -> anyhow::Result<RestApiResponse<models::SignUsEquityDisclaimerResponse>>;
}

#[derive(Debug, Clone)]
pub struct AccountApiClient {
    configuration: ConfigurationRestApi,
}

impl AccountApiClient {
    pub fn new(configuration: ConfigurationRestApi) -> Self {
        Self { configuration }
    }
}

/// Request parameters for the [`sign_us_equity_disclaimer`] operation.
///
/// This struct holds all of the inputs you can pass when calling
/// [`sign_us_equity_disclaimer`](#method.sign_us_equity_disclaimer).
#[derive(Clone, Debug, Builder, Deserialize, Default)]
#[builder(pattern = "owned", build_fn(error = "ParamBuildError"))]
pub struct SignUsEquityDisclaimerParams {
    /// The value cannot be greater than `60000`.
    ///
    /// This field is **optional.
    #[builder(setter(into), default)]
    #[serde(rename = "recvWindow", default)]
    pub recv_window: Option<i64>,
}

impl SignUsEquityDisclaimerParams {
    /// Create a builder for [`sign_us_equity_disclaimer`].
    ///
    #[must_use]
    pub fn builder() -> SignUsEquityDisclaimerParamsBuilder {
        SignUsEquityDisclaimerParamsBuilder::default()
    }
}

#[async_trait]
impl AccountApi for AccountApiClient {
    async fn sign_us_equity_disclaimer(
        &self,
        params: SignUsEquityDisclaimerParams,
    ) -> anyhow::Result<RestApiResponse<models::SignUsEquityDisclaimerResponse>> {
        let SignUsEquityDisclaimerParams { recv_window } = params;

        let mut query_params = BTreeMap::new();
        let body_params = BTreeMap::new();

        if let Some(rw) = recv_window {
            query_params.insert("recvWindow".to_string(), json!(rw));
        }

        send_request::<models::SignUsEquityDisclaimerResponse>(
            &self.configuration,
            "/sapi/v1/equity/account/disclaimer",
            reqwest::Method::POST,
            query_params,
            body_params,
            if HAS_TIME_UNIT {
                self.configuration.time_unit
            } else {
                None
            },
            true,
        )
        .await
    }
}

#[cfg(all(test, feature = "stocks"))]
mod tests {
    use super::*;
    use crate::TOKIO_SHARED_RT;
    use crate::{errors::ConnectorError, models::DataFuture, models::RestApiRateLimit};
    use async_trait::async_trait;
    use std::collections::HashMap;

    struct DummyRestApiResponse<T> {
        inner: Box<dyn FnOnce() -> DataFuture<Result<T, ConnectorError>> + Send + Sync>,
        status: u16,
        headers: HashMap<String, String>,
        rate_limits: Option<Vec<RestApiRateLimit>>,
    }

    impl<T> From<DummyRestApiResponse<T>> for RestApiResponse<T> {
        fn from(dummy: DummyRestApiResponse<T>) -> Self {
            Self {
                data_fn: dummy.inner,
                status: dummy.status,
                headers: dummy.headers,
                rate_limits: dummy.rate_limits,
            }
        }
    }

    struct MockAccountApiClient {
        force_error: bool,
    }

    #[async_trait]
    impl AccountApi for MockAccountApiClient {
        async fn sign_us_equity_disclaimer(
            &self,
            _params: SignUsEquityDisclaimerParams,
        ) -> anyhow::Result<RestApiResponse<models::SignUsEquityDisclaimerResponse>> {
            if self.force_error {
                return Err(ConnectorError::ConnectorClientError {
                    msg: "ResponseError".to_string(),
                    code: None,
                }
                .into());
            }

            let resp_json: Value = serde_json::from_str(r#"{"success":true}"#)
                .unwrap_or_else(|_| serde_json::json!({}));
            let dummy_response: models::SignUsEquityDisclaimerResponse =
                serde_json::from_value(resp_json.clone())
                    .expect("should parse into models::SignUsEquityDisclaimerResponse");

            let dummy = DummyRestApiResponse {
                inner: Box::new(move || Box::pin(async move { Ok(dummy_response) })),
                status: 200,
                headers: HashMap::new(),
                rate_limits: None,
            };

            Ok(dummy.into())
        }
    }

    #[test]
    fn sign_us_equity_disclaimer_required_params_success() {
        TOKIO_SHARED_RT.block_on(async {
            let client = MockAccountApiClient { force_error: false };

            let params = SignUsEquityDisclaimerParams::builder().build().unwrap();

            let resp_json: Value = serde_json::from_str(r#"{"success":true}"#)
                .unwrap_or_else(|_| serde_json::json!({}));
            let expected_response: models::SignUsEquityDisclaimerResponse =
                serde_json::from_value(resp_json.clone())
                    .expect("should parse into models::SignUsEquityDisclaimerResponse");

            let resp = client
                .sign_us_equity_disclaimer(params)
                .await
                .expect("Expected a response");
            let data_future = resp.data();
            let actual_response = data_future.await.unwrap();
            assert_eq!(actual_response, expected_response);
        });
    }

    #[test]
    fn sign_us_equity_disclaimer_optional_params_success() {
        TOKIO_SHARED_RT.block_on(async {
            let client = MockAccountApiClient { force_error: false };

            let params = SignUsEquityDisclaimerParams::builder()
                .recv_window(5000)
                .build()
                .unwrap();

            let resp_json: Value = serde_json::from_str(r#"{"success":true}"#)
                .unwrap_or_else(|_| serde_json::json!({}));
            let expected_response: models::SignUsEquityDisclaimerResponse =
                serde_json::from_value(resp_json.clone())
                    .expect("should parse into models::SignUsEquityDisclaimerResponse");

            let resp = client
                .sign_us_equity_disclaimer(params)
                .await
                .expect("Expected a response");
            let data_future = resp.data();
            let actual_response = data_future.await.unwrap();
            assert_eq!(actual_response, expected_response);
        });
    }

    #[test]
    fn sign_us_equity_disclaimer_response_error() {
        TOKIO_SHARED_RT.block_on(async {
            let client = MockAccountApiClient { force_error: true };

            let params = SignUsEquityDisclaimerParams::builder().build().unwrap();

            match client.sign_us_equity_disclaimer(params).await {
                Ok(_) => panic!("Expected an error"),
                Err(err) => {
                    assert_eq!(err.to_string(), "Connector client error: ResponseError");
                }
            }
        });
    }
}
